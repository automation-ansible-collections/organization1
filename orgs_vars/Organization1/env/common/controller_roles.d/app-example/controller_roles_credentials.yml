---
controller_roles:
  - credentials:
      - "{{ orgs }} {{ env }} Machine Ansible Privileged"
    team: "ldap-org01-users"
    role: use

  - credentials:
      - "{{ orgs }} {{ env }} Machine Ansible Privileged"
      - "{{ orgs }} {{ env }} Machine Ansible Non-Privileged"
    team: "ldap-org01-devs"
    role: use
...
